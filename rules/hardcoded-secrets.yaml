rules:
  - id: hardcoded-secret
    languages: [generic]
    message: Possible hardcoded secret.
    severity: WARNING
    patterns:
      - pattern-regex: (password|passwd|secret|api_key)\s*=\s*["'][^"']+["']
